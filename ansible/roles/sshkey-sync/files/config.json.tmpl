{
    "system": [
        {"user": "{{ system_username }}", "authorized_keys_file": "{{ user.home }}/.ssh/authorized_keys"}
    ],
    "github": {
        "url": "api.github.com",
        "accounts": [
            {"username": "{{ github_username }}", "system_user": "{{ system_username }}"}
        ]
    }
}
